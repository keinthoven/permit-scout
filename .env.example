# Local: copy to `.env`. Vercel: Project → Settings → Environment Variables (Production at minimum; add per Preview branch if you use PR previews).
# RIDB keys: https://ridb.recreation.gov/
VITE_RIDB_API_KEY=your_ridb_api_key_here
